[build]
  command = "npm run build"
  publish = "dist"

# SPA: qualquer rota cai no index.html (o React Router, se você adicionar depois, cuida do resto)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
